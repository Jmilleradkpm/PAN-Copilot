# PAN Copilot — Local Desktop requirements

anthropic>=0.28.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
httpx>=0.27.0
cryptography>=42.0.0

# Migration engine (dev: pytest)
pytest>=8.0.0

# Build tool — install separately, not bundled into the app
# pip install pyinstaller
